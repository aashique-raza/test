function add(a,b){
  return a+b;
}

const addnumber=add(2,3);
console.log(addnumber);

function getOTP(){
const otp=Math.floor(Math.random()*999999)
  return otp;
}
const otp=getOTP()
console.log(otp)
